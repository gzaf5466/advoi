# Backend environment configuration
# Rename to .env and adjust values

# Port for Express server
PORT=5000

# Allowed client origin for CORS (e.g., http://localhost:5173)
CLIENT_ORIGIN=http://localhost:5173

# JWT settings
JWT_SECRET=change_this_in_production
JWT_EXPIRES_IN=7d

# PostgreSQL settings
# e.g., postgres://USER:PASSWORD@HOST:PORT/DBNAME
DATABASE_URL=
# Some providers require SSL; set to true on Render/Neon/Supabase
DATABASE_SSL=true
